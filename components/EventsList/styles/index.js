import styled from "styled-components";
import { flexDisplayCenter } from "../../common/tokens";

export const EventsListContainer = styled.div`
  ${flexDisplayCenter()};
  flex-direction: row;
  flex-wrap: wrap;
  margin: 0 10%;
`;
